{
  "name": "calendar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "file-saver": "^2.0.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "calendar-deck": "^0.1.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build-ics",
    "build-ics": "calendar-deck build --outputFormat=ics --outputPath=./dist ./data/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.10"
  }
}
